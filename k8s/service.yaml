apiVersion: v1
kind: Service
metadata:
  name: ci-cd-pipeline-service
spec:
  type: LoadBalancer
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
  selector:
    app: ci-cd-pipeline-system
